query GetProjects {
    projects(id: "1") {
        id
        name
        locations {
            name
            id
            create
            screens {
                temperature
            }
        }
    }
    roles {
        id
        name
    }
}
